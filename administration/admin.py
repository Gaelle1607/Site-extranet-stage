"""
=============================================================================
ADMIN.PY - Configuration de l'interface d'administration Django native
=============================================================================

Ce fichier permet d'enregistrer des modèles dans l'interface d'administration
Django native (/admin/). Actuellement vide car l'application administration
utilise une interface personnalisée plutôt que l'admin Django.

L'interface d'administration personnalisée est accessible via :
    /administration/

Note : L'admin Django native reste disponible pour les superusers à /admin/
pour des tâches de maintenance avancées.

Projet : Extranet Giffaud Groupe
=============================================================================
"""
from django.contrib import admin

# Aucun modèle enregistré dans l'admin Django native pour cette application
# L'interface d'administration personnalisée est utilisée à la place
